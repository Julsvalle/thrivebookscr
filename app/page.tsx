// Root page — next-intl proxy handles locale redirects automatically.
// This file is kept as a fallback (should never be reached in normal flow).
export default function RootPage() {
  return null;
}
